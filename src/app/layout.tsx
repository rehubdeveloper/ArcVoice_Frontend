import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Arc Voice - An emotionally intelligent AI assistant that enables human-like conversations for counseling, guidance, and automated workflows, using advanced LLMs, voice tech, and smart integrations.",
  description: "Arc Voice is an AI assistant designed to facilitate human-like conversations for counseling, guidance, and automated workflows. It leverages advanced large language models (LLMs), voice technology, and smart integrations to provide empathetic and effective support.",
};


export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
